const obj = {width: 10, height: 15};
const area = obj.width * obj.height

console.log(4 / [])

interface User {
    name: string,
    id: number,
}

const user: User = {
    name: "Hayes",
    id: "a"
}